{
  "name": "@elektroplan/calculation-data",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b && node ./scripts/copy-json.mjs",
    "build:materials": "node scripts/build-materials-seed.mjs",
    "typecheck": "tsc -b --pretty false",
    "lint": "eslint src --ext .ts",
    "test": "tsc -b && node ./scripts/copy-json.mjs && node ./tests/run-load-json-dataset.mjs && node ./tests/run-iec-datasets.mjs && node ./tests/run-materials-seed.mjs && node ./tests/run-materials-dataset.mjs && vitest run --configLoader native"
  },
  "dependencies": {
    "@elektroplan/contracts": "workspace:*",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "vitest": "^3.1.2"
  }
}
